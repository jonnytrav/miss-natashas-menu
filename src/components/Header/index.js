import React from "react";

const Header = () => {
  return (
    <div>
      <nav>
        <a>Home</a>
        <a>Menu</a>
        <a>About</a>
      </nav>
    </div>
  );
};

export default Header;
